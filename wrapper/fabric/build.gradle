import net.fabricmc.loom.util.ModPlatform

apply from: rootProject.file("gradle/scripts/klib-fabric.gradle")

base.archivesName = rootProject.name

var modules = ["base",
               "command",
               "config",
               "data",
               "entity-events",
               "entrypoint",
               "event-base",
               "keybinding",
               "lifecycle-events",
               "platform",
               "registry"]

kessoku {
    moduleIncludes(modules, ModPlatform.FABRIC.id())
}
