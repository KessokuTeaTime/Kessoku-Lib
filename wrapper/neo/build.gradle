apply from: rootProject.file("gradle/scripts/klib-neo.gradle")

base.archivesName = rootProject.name

var modules = ["base",
               "command",
               "config",
               "data",
               "entity-events",
               "entrypoint",
               "event-base",
               "keybinding",
               "lifecycle-events",
               "platform",
               "registry"]

kessoku {
    moduleIncludes(modules, "neo")
}
