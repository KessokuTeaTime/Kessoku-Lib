import band.kessoku.scripts.gradle.plugin.PlatformIdentifier

apply from: rootProject.file("gradle/scripts/klib-neo.gradle")

base.archivesName = rootProject.name + "-blockentity"

kessoku {
    common("blockentity", PlatformIdentifier.NEO)
    shadowBundle("blockentity", PlatformIdentifier.NEO)
}
