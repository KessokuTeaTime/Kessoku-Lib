import net.fabricmc.loom.util.ModPlatform

apply from: rootProject.file("gradle/scripts/klib-fabric.gradle")

base.archivesName = rootProject.name + "-transfer"

kessoku {
    module("base", "common")

    common("transfer", ModPlatform.FABRIC)
    shadowBundle("transfer", ModPlatform.FABRIC)
}
